---
# ============================================================
#  kill-job.yml — прибивает висящий flink CLI driver на JM-ноде.
#
#  Когда make submit-* запущено в attached mode и ansible прибит
#  Ctrl-C-ом, удалённый flink run (CliFrontend) остаётся висеть из-за
#  SSH ControlMaster-а и продолжает слать INSERT-ы. Эта playbook-а
#  идёт на JM-хост и убивает процесс.
#
#  Почему ps aux, а не pkill/pgrep -f:
#    pkill/pgrep -f читают /proc/<pid>/cmdline, у которого есть лимит
#    (~4КБ). У flink run classpath — десятки КБ, и main-класс
#    org.apache.flink.client.cli.CliFrontend оказывается за пределами
#    читаемой области → pkill его не находит. `ps aux` же тянет argv
#    целиком и показывает класс → grep его ловит, PID извлекаем awk.
#
#  Параметры:
#    kill_class     — точный main-класс (default: TpcdsBenchmarkJob).
#    kill_all       — true: прибить ЛЮБОЙ CliFrontend (все flink run-ы).
#
#  Примеры:
#    ansible-playbook playbooks/kill-job.yml
#    ansible-playbook playbooks/kill-job.yml -e kill_class=PaimonHdfsJob
#    ansible-playbook playbooks/kill-job.yml -e kill_all=true
# ============================================================

- name: Kill flink CLI driver on JobManager
  hosts: jobmanager
  gather_facts: false
  vars:
    kill_class: TpcdsBenchmarkJob
    kill_all: false
  tasks:
    - name: Find matching flink CLI processes (via ps aux)
      shell: |
        if [ "{{ kill_all | bool }}" = "True" ]; then
          ps aux | grep CliFrontend | grep -v grep | awk '{print $2}'
        else
          ps aux | grep CliFrontend | grep '{{ kill_class }}' | grep -v grep | awk '{print $2}'
        fi
      register: _pids
      changed_when: false

    - name: Show what will be killed
      shell: |
        if [ "{{ kill_all | bool }}" = "True" ]; then
          ps aux | grep CliFrontend | grep -v grep || echo 'nothing to kill'
        else
          ps aux | grep CliFrontend | grep '{{ kill_class }}' | grep -v grep || echo 'nothing to kill'
        fi
      register: _pids_details
      changed_when: false

    - name: Plan
      debug:
        msg: "{{ _pids_details.stdout_lines }}"

    - name: Send SIGTERM, wait 3s, SIGKILL leftovers
      shell: |
        PIDS="{{ _pids.stdout_lines | join(' ') }}"
        if [ -z "$PIDS" ]; then
          echo "no PIDs to kill"
          exit 0
        fi
        echo "killing: $PIDS"
        kill -TERM $PIDS 2>/dev/null || true
        sleep 3
        kill -KILL $PIDS 2>/dev/null || true
        echo "done: $PIDS"
      when: _pids.stdout | length > 0
      register: _kill_result

    - name: Kill result
      debug:
        msg: "{{ _kill_result.stdout | default('nothing killed') }}"
